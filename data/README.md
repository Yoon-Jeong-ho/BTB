# Data

실제 데이터는 대부분 크기 때문에 Git에 직접 넣지 않는다. 이 폴더는 데이터 저장 위치와 규칙을 고정하기 위한 자리다.

## 원칙

- `raw/`: 원본 데이터. 수정 금지.
- `interim/`: 전처리 중간 산출물.
- `processed/`: 학습에 바로 쓰는 포맷.
- `external/`: 외부에서 받은 보조 데이터.

작은 샘플 데이터가 꼭 필요할 때만 별도로 커밋하고, 그렇지 않으면 README와 다운로드 스크립트/문서만 남긴다.
