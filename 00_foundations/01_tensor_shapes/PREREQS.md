# 01 Tensor Shapes 선행 개념

## 꼭 알고 오면 좋은 것
- 벡터와 행렬 표기
- 행렬 곱의 조건
- 축(axis)과 차원(dimension)이라는 말의 기본 의미

## 빠른 자기 점검
- `(2, 3)`과 `(3, 4)`를 곱하면 결과 shape를 말할 수 있는가?
- `(2, 3)` 텐서에 `(4,)` 벡터를 더하려고 할 때 왜 실패하는지 설명할 수 있는가?
