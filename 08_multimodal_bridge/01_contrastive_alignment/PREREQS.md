# 선수 지식

## 이미 알고 있으면 좋은 것
- 벡터 내적과 cosine similarity의 차이
- softmax / cross entropy가 점수 행렬에서 어떻게 동작하는지
- `(batch, dim)` 과 `(batch, batch)` shape를 읽는 기본 감각

## 이 단위에서 새로 가져갈 것
- 이미지와 텍스트가 서로 다른 encoder를 거쳐도 같은 embedding space에서 비교될 수 있다는 관점
- similarity matrix를 retrieval 관점으로 읽는 법
- 대각선 정답 쌍, 비대각선 hard negative, temperature의 역할

## 막히면 먼저 복습할 것
- `00_foundations/01_tensor_shapes`
- `00_foundations/02_activation_and_loss`
- `02_nlp_bridge/01_tokenization_and_embeddings`
