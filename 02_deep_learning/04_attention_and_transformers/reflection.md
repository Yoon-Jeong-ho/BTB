# 04 Attention and Transformers 회고

- 이번 실습에서 attention을 “토큰 선택”이 아니라 **sequence mixing**으로 이해하게 된 순간은 언제였는가?
- row sum이 1이라는 사실이 왜 중요했는지, 내 말로 다시 설명해 보라.
- 두 head가 서로 다른 top key를 보였을 때 어떤 해석이 가능했는가?
- encoder와 decoder 차이를 **미래 토큰 접근 가능 여부**로 한 문장으로 요약해 보라.
- transformer가 recurrent bottleneck을 줄인다는 말과, 길이 제곱 비용이 남는다는 말을 동시에 어떻게 받아들일 것인가?
- BERT류 / GPT류 / encoder-decoder seq2seq를 각각 어디에 놓을지 다시 적어 보라.
